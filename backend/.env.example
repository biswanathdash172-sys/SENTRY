# Copy this to .env (or export these manually) before running locally.
# NEVER commit real values — this file only documents what's needed.

# --- Admin auth ---
JWT_SECRET=biswanath
JWT_EXPIRE_HOURS=12
ADMIN_INVITE_CODE=123#

# --- Pre-created test admin (used by db/seed_org.py) ---
SENTRY_ORG_NAME=Sentry Demo Org
SENTRY_ADMIN_USERNAME=admin
SENTRY_ADMIN_PASSWORD=ChangeMe123!

# --- Gmail IMAP (monitored mailbox) ---
# Generate an App Password at https://myaccount.google.com/apppasswords
# (requires 2-Step Verification enabled on the Google account).
IMAP_HOST=imap.gmail.com
IMAP_USER=biswanathdash172@gmail.com
IMAP_APP_PASSWORD= fcde txud ohcx gwqv
POLL_INTERVAL_SECONDS=30

# --- Poller -> API connection ---
SENTRY_API_BASE=http://localhost:8000
