pyyaml==5.3.1
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
sqlalchemy==2.0.35
filelock==3.16.1
requests==2.32.3
python-multipart==0.0.12
python-dotenv==1.0.1
supabase==2.7.4
 python-pptx==1.0.2
 google-auth>=2.0.0
google-auth-oauthlib>=0.6.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.0.0
# psycopg2-binary is OPTIONAL - only needed if you set DATABASE_URL to a
# real Postgres instance. Without it, the app automatically falls back to
# in-memory storage (see backend/db/database.py) and boots fine. Uncomment
# only if you actually have Postgres configured AND your platform has a
# prebuilt wheel for it (Windows/py3.13 often doesn't yet - this was
# causing the "pg_config not found" build error).
# psycopg2-binary==2.9.9

# --- Gmail API (OPTIONAL — only needed for Gmail API poller in gmail_poller.py)
# The backend and dashboard degrade gracefully if these are not installed
# (IMAP fallback is used automatically). Install only if you have credentials.json.
# google-auth==2.35.0
# google-auth-oauthlib==1.2.1
# google-auth-httplib2==0.2.0
# google-api-python-client==2.149.0